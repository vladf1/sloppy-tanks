# Vite gives every file in /assets/ a content hash in its filename.
# Keep HTML and unversioned public files on Cloudflare Pages' default revalidation policy.
/assets/*
  Cache-Control: public, max-age=31536000, immutable
